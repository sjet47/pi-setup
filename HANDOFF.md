### TL;DR
`pi-search-prompts` 使用 Ctrl+Up 打开历史 prompt 搜索，避免覆盖 Pi 的 `app.session.rename`（Ctrl+R）和普通 Up 光标移动。

### 验证
- prompt 历史按时间正序展示，最新项在底部且会默认选中。
- 新插件严格 TypeScript 检查、语法检查和 `git diff --check` 均通过。
